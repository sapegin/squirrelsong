import { ReactNode } from 'react';

interface Props {
  as?: string;
  display?: string;
  direction?: string;
  gap?: string;
  alignItems?: string;
  variant?: string;
  level?: number;
  children?: ReactNode;
}

export const Text = (props: Props) => null;
export const Stack = (props: Props) => null;
export const Heading = (props: Props) => null;
